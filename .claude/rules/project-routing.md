When Patrick mentions a project keyword, check the routing table in ~/projects/CLAUDE.md to find the project path. Load that project's CLAUDE.md for details. If deep reference is needed (transcripts, emails, API docs), check the Deep Reference Index in ~/projects/CLAUDE.md.

When Patrick mentions a person, check the Key People table in ~/projects/CLAUDE.md to find their project(s). Load those project CLAUDE.md files. Check ~/projects/meeting-transcripts/{person}/ for conversation history if needed.

For cross-project tasks, load both project CLAUDE.md files and check the Cross-Project Relationships section.
